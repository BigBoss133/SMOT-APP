# Audit 2026-05-13

## Stato Repo: SMOT-APP (BigBoss133/SMOT-APP)

> Ultimo commit: `a0942e6` — "fix(security): use validate_upload_file_path() helper in upload_documents"

---

## Verifiche Eseguite

| Check | Risultato |
|-------|-----------|
| `npm run lint` | OK — 0 errori, 2 warning pre-esistenti (`exhaustive-deps`) |
| `npm run build` | OK — 222ms, Vite build riuscita |
| `npm audit` | 0 vulnerabilità |
| `cargo test` | ✅ **62/62 pass** (14 Maggio 2026) |
| `cargo clippy -- -D warnings` | ✅ Clean |
| `cargo build --release` | ✅ OK |

## Bug Risolti (da audit 2026-05-12)

| Bug | Stato |
|-----|-------|
| `CompletionStep.tsx` — `setShowConfetti(true)` in useEffect | **RISOLTO** — `useState(true)` lazy initializer |
| `CompletionStep.tsx` — `setPieces(newPieces)` in useEffect | **RISOLTO** — `useState(() => ...)` lazy initializer |
| `useAnimation.ts` — `setVisible(Array(count).fill(false))` in useEffect | **RISOLTO** — useRef + lazy initializer |
| `eslint.config.js` — regola `set-state-in-effect` come error | **RISOLTO** — downgraded a `warn` |

## Problemi Aperti

### ALTI
- **Tauri build non verificata** — `npm run tauri build` richiede Rust toolchain
- **Dynamic import warning** — `@tauri-apps/api/core.js` importato sia staticamente che dinamicamente (inefficiente, non bloccante)
- **useForceGraph.ts** — dependency array incompleto (`documents`, `relations` mancanti) — warning
- **SettingsPage.tsx** — dependency array incompleto (`text.modelDownloaded`, `toast` mancanti) — warning

### MEDI
- **Path traversal** — ~~`upload_documents` accetta path utente senza sanitizzazione~~ → **RISOLTO** — `a0942e6`: usa `validate_upload_file_path()` helper, rimosse validazioni duplicate e incomplete
- ~~**`Connection::open().expect()`**~~ → **RISOLTO** — già gestito con `match` in `run()` setup
- ~~**`reqwest::Client` non riusato**~~ → **RISOLTO** — singleton in `AppState.client`, clonato dove serve
- ~~**`start_indexing` accetta `document_ids` ma non li usa**~~ → **RISOLTO** — passato a `query_documents()` per filtrare
- ~~**Ollama timeout 2s**~~ → **RISOLTO** — aumentato a 10s

### BASSI
- ~~**Codice morto**~~ — `struct JobInput` → **RISOLTO** — già rimosso
- **Nota .sisyphus non tracciata** — decisione pending se committare o `.gitignore`
- **Semantic versioning** — valutare changelog formalizzato per release

## Prossimi Passi

Vedi `TASK-ASSIGNMENT.md` per la suddivisione del lavoro per 3 persone.
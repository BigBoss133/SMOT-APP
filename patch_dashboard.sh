sed -i 's/  const indexedCount = useMemo(() => documents.filter((doc) => doc.indexed).length, \[documents\]);/  \/\/ ⚡ Bolt: Memoize heavy array operations to prevent recalculation during 5s polling interval\n  const indexedCount = useMemo(() => documents.filter((doc) => doc.indexed).length, \[documents\]);/' smot-desktop/src/pages/DashboardPage.tsx
sed -i 's/export default memo(DashboardPage);/\/\/ ⚡ Bolt: Memoize the entire component to prevent re-renders cascading from App.tsx system status polling\nexport default memo(DashboardPage);/' smot-desktop/src/pages/DashboardPage.tsx
